# Handoff — 2026-07-07 (Revenue Push Session)

**Read this first.** Mission: **₱8,000 revenue by July 31, 2026** (covers Claude subscription, Supabase, sunk time). I am acting as UX/conversion officer for this goal. Baseline honesty: at July 6 there was exactly **one real sale ever** (₱49 CP1, thatonesei.so@gmail.com, Jul 3). Everything else in payments/subscriptions is the owner's own tests, a demo row, or a manual grant.

## The data that drove everything (as of 2026-07-06)

- Launch spike Jun 16 (~1,300 devices/day) decayed to ~160/day. 78% of ~5,100 all-time devices visited exactly one day.
- CP1 = 42% of all visitors (2,156 devices). Demand is heavily 1st-Year.
- 3,143 devices hit gated modules; conversion was ~0.03%.
- 92 auth accounts, 82 waitlist emails, 8 users stuck unconfirmed (see memory `project_pending_user_outreach`).
- **Critical bug found & fixed:** the live `events.event_type` CHECK constraint silently rejected `paywall_teaser_view` / `paywall_teaser_click` / `subscribe_click` since launch (the API ignored insert errors). We had ZERO funnel data before Jul 6.

## What shipped this session (all on main, deployed ● Ready)

1. **`9f4ab58` — analytics unblocked.** Constraint widened (migration `20260706150000_events_widen_type_add_attribution.sql`, applied live), insert errors now `console.error`'d, `enter` events capture `referrer` + `utm_source/medium/campaign`. Funnel events verified flowing within 30 min.
2. **`70e7ae7` — CP1 conversion surgery** (spec/plan: `docs/superpowers/specs|plans/2026-07-06-cp1-conversion*`):
   - **3-tier ladder:** `subject_month` ₱49/31d · `subject_sem` ₱99/until SEMESTER_END ★Most Popular · `year_sem` ₱299 all-subjects-in-year/until SEMESTER_END. Single source of truth: `PLANS` in `lib/paymongo.ts`. `SEMESTER_END = 2026-12-31T15:59:59Z` — **must be bumped each semester**; semester grants floor at now+31d so a stale constant never under-grants.
   - Links carry `plan:<key>` in remarks; webhook + admin reconcile resolve via `resolvePlan()` (legacy tokenless links: subject→subject_month, year→year_sem) and pass `periodEnd: periodEndFor(plan)` to `recordPayment`. Underpayment-only rejection preserved.
   - **Reviewer reframe:** all locked-user copy says "reviewers with answer keys", never "activities". "Cancel anytime" (was false — payments are one-time links) replaced with "One-time payment… No auto-renew".
   - **Free first reviewer:** each subject's first activity (`pickFirstActivity` in `lib/freeSample.ts`) renders in full for locked visitors + upsell banner ("That was 1 of N…"). Verified via anonymous curl that ONLY that section's body leaves the server.
   - Price labels are hardcoded in **three** client components — `SubscribeGate.tsx`, `PaywallTeaser.tsx`, `components/account/AccountSidebar.tsx` — keep in sync with `PLANS` (client components can't import lib/paymongo.ts; it pulls node:crypto).
3. **`6cdc4b4`** — badge overlap fix on gate cards (badges now in-flow, not absolute).
4. **`1456ba4`** — `docs/marketing/2026-07-tiktok-revenue-campaign.md` ("Basahin Mo Muna", July 7–31, 1 post/day, sell-without-selling funnel).

Quality state: 194 vitest tests green (TDD on all money paths), lint clean, prod build green, visually verified in browser as a locked visitor (teaser, free sample, 3-card gate).

## ⚠️ Open decision (blocks campaign copy, ask the user first)

The TikTok plan promises "**₱299 one-time, no expiry**" but the app sells **₱299/semester until Dec 31**. Recommended: fix the campaign doc to semester framing (keeps January renewal revenue). Alternative: make `year_sem` non-expiring. One of the two must change before posting starts.

## Next work, in priority order

1. **Read the funnel (~Jul 10-11, after 3-5 days of data):** `paywall_teaser_view → paywall_teaser_click → subscribe_click` per subject from `events`, vs `payments` ledger; `utm_source` on `enter` shows which shares convert. Tune whatever leaks.
2. **Block/class-rep sales (fastest path to ₱8k, not built):** ₱999/block/subject-sem, ₱1,999/block/all — needs promo-code or manual-grant flow (admin reconcile pattern exists) + a "message us" CTA. ~4-8 block deals = goal met.
3. **Reactivation:** auto-confirm the 8 stuck users, one plain-text email to 92 accounts + 82 waitlist ("Prelim reviewers live for [their most-opened subject]" — derivable from `events`).
4. **Optional smoke test:** one real ₱99 purchase end-to-end (check `subscriptions.current_period_end` = Dec 31). Admin reconcile view is the safety net.
5. **Deferred UX idea:** when a module has many locked activities, each renders a full 3-card gate — consider compact gates after the first.

## Gotchas / project conventions

- **Freeze non-revenue work** (2nd–4th-yr content, dashboard polish) until the goal is hit — user is credit-conscious; work inline, no subagents.
- Supabase project `mpdymglipgzuybtxuvhy` (ap-southeast-1). **Live DB and repo migrations have diverged before** — check live schema via MCP before writing migrations, and apply via `apply_migration` + mirror an idempotent repo file.
- Push via `git push "https://$(gh auth token)@github.com/lauurnce/survivalKitApp.git" main`. NOTE: this does NOT update the local `origin/main` tracking ref — run `git fetch` before trusting `git log origin/main..main` (this caused a false "force-push panic" this session).
- No Co-Authored-By trailers ever; lauurnce is sole author. Vercel auto-deploys main; after pushing check `vercel ls --prod` for `● Error` (a failed build silently serves the old deploy — bit us on 2026-06-30).
- Full context also in auto-memory: `project_revenue_push_july2026`, `project_payment_pipeline` (updated with the 3-tier model).
