# Plan: next 15.5 → 16.3.2 supply-chain bump

- **Track:** opencode-C (parallel fan-out 2026-08-25)
- **Worktree:** `~/projects/survivalKitApp-next16` · branch `chore/next-16-bump`
- **Base:** `4874a98`
- **Scope guard:** owned globs only — `package.json`, `package-lock.json`,
  `next.config.*`, plus `app/**` `components/**` `lib/**` `middleware.ts`
  fallout edits strictly required by the bump. New dependencies or config
  redesigns = stop and report.

## Goal

Clear the last high-severity supply-chain finding: 3 high/critical advisories
whose fixed versions are unreachable inside `next@15` because the vulnerable
transitive pins ride inside Next's own spec. WARDEN verified only `next@16.3.2`
clears them. `eslint-config-next` moves in lockstep. React `^19` compatible.

## Facts

- Current: `next ^15.5.19`, `eslint-config-next ^15.5.19`, `react ^19.0.0`,
  `react-dom ^19.0.0`, `eslint ^9`, `typescript ^5`, Node engines `24.x`
  (`.nvmrc` 24) — satisfies Next 16's Node ≥ 20.9 floor.
- App Router repo. `middleware.ts` sets per-request nonce CSP + Supabase cookie
  refresh + admin guards. `next.config.ts`: `poweredByHeader`,
  `serverExternalPackages`, `outputFileTracingIncludes`, scoped `headers()`.
  No `images` block, no custom webpack/babel, no AMP, no PPR flags.
- Gates: `npm run typecheck` (tsc --noEmit) · `npm run test` (vitest run,
  ~1462 passing across ~116 files expected) · `npm run lint` (**`next lint` —
  command removed in Next 16, script will need migration**) · `npm run build`.
- Known pre-flagged risk: `npm run lint` = `next lint`; Next 16 removed the
  built-in lint command. Migration path: ESLint CLI. Repo lints via
  `.eslintrc.json` (legacy eslintrc format) + `.eslintignore`; `@eslint/eslintrc`
  already a devDep for FlatCompat if flat config conversion is required.
- Isolation rule: node_modules installed **inside** this worktree (no symlink);
  `.env.local` is a symlink to main checkout (read-only use).

## Steps

1. [x] This plan doc committed before any code change.
2. [ ] Record pre-state: `npm ls next eslint-config-next react` +
       `npm audit --json` high-severity summary (below).
3. [ ] `npm install next@16.3.2 eslint-config-next@16.3.2` (lockfile update;
       allow react/react-dom peer-driven patch/minor drift only within ^19).
       Conventional commit for the bump.
4. [ ] Sweep breaking changes (checklist below), logging every area checked.
5. [ ] Full gate suite after bump and after every fix batch
       (`typecheck && test && lint && build`). Small conventional-commit
       batches: `fix(next16): …`.
6. [ ] Post-gates proof: `npm audit` highs gone; capture final
       `npm ls next eslint-config-next react`.
7. [ ] Leave branch ready for review. NO push, NO merge.

## Breaking-change checklist

Sources: official upgrade guide `nextjs.org/docs/app/guides/upgrading/version-16`
(fetched at 16.3.2 docs), swept against the tree at bump commit `aff91f6`.

| Area | Status | Notes |
|---|---|---|
| Node engine requirement | ✅ CLEAN | Next 16 floor Node ≥20.9 / TS ≥5.1; repo pins engines `24.x`, `.nvmrc` 24, `typescript ^5` |
| React version floor | ✅ CLEAN | Next 16 pairs with React 19.2; resolved react/react-dom 19.2.7 unchanged by bump (no peer conflict) |
| async request APIs | ✅ CLEAN | All 21 `cookies()`/`headers()` call sites awaited; every page/route `params`/`searchParams` typed `Promise<>` + `await`/`use` (15-era migration complete); API routes use `req.nextUrl.searchParams` (NextRequest, unaffected) |
| async params/id for icon & OG image fns | ✅ CLEAN | `app/icon.tsx` / `app/apple-icon.tsx` accept no params; no opengraph-image/twitter-image files; no `generateImageMetadata` |
| async sitemap id | ✅ CLEAN | `app/sitemap.ts` has no `generateSitemaps`; takes no args |
| caching APIs (`revalidateTag` 2nd arg, `updateTag`, `cacheLife/cacheTag`) | ✅ CLEAN | Zero usages of revalidateTag/updateTag/unstable_cache/unstable_cacheLife/unstable_cacheTag anywhere; only route-segment config `export const revalidate = 3600` on sitemap (still supported) |
| fetch / GET route handler caching defaults | ✅ CLEAN (behavioral note) | No `fetch` cache/revalidate overrides in lib; GET handlers uncached-by-default since 15 → semantics identical in 16 |
| PPR flags (`experimental.ppr`, `experimental_ppr`) | ✅ CLEAN | Not used |
| experimental.dynamicIO / useCache / unstable_rootParams | ✅ CLEAN | Not used |
| AMP removal | ✅ CLEAN | No amp config/imports |
| runtimeConfig / getConfig | ✅ CLEAN | Not used |
| devIndicators options | ✅ CLEAN | Not used |
| Parallel routes default.js requirement | ✅ CLEAN | No `@slot` directories under app/ |
| middleware → proxy rename | ⚠️ KEPT middleware (supported path) | Guide: filename deprecated, NOT removed; edge-runtime proxy unsupported ("keep using middleware" if edge wanted). middleware.ts uses Web Crypto + btoa/atob (Edge-safe). Decision: keep `middleware.ts` + `middleware()` on Edge for this supply-chain-focused bump; adopt `proxy.ts`+Node runtime as a deliberate follow-up (auth-critical file; secrets track historically owns deviceCookie/middleware work). Deprecation warnings may appear in build output |
| Turbopack default bundler | ✅ VERIFIED VIA GATE | No custom webpack/babel config anywhere → nothing to trip the webpack-config build failure. `scripts/run-next.mjs` spawns plain `next dev`/`next start` (no bundler flags); watch build log for tracing/loader surprises. `.next/dev` split + single-instance lockfile: this worktree is an isolated dir, no contention with other tracks |
| next/image changes | ✅ CLEAN (defaults shifted) | No `<Image>` local-src query strings (localPatterns.search n/a); no quality props (default [75] n/a); no next/legacy/image; no images.domains. Defaults that silently changed: minimumCacheTTL 60s→4h, imageSizes drops 16px, maximumRedirects→3, local-IP optimization blocked — none reachable by this app's static `/public`-only images |
| scroll-behavior override removal | ✅ CLEAN | No `scroll-behavior` CSS in app/components |
| Concurrent dev/build dirs | ✅ CLEAN | Isolated worktree dir |
| `next lint` removal | ❌ HIT — fix required | `npm run lint` = `next lint` (command removed; build no longer lints either). Fix in first fallout batch: ESLint CLI via package.json script within owned globs. Legacy `.eslintrc.json` extends paths must still resolve under eslint-config-next@16 or a FlatCompat flat config is required |
| next.config options removed/renamed | ✅ CLEAN | `poweredByHeader`, `serverExternalPackages`, `outputFileTracingIncludes`, `headers()` all remain valid top-level API in 16; no `eslint` key present |

## Pre-state (recorded at base `4874a98` + plan commit `61a186e`)

```
$ npm ls next eslint-config-next react react-dom
├── eslint-config-next@15.5.19
├─┬ next@15.5.23            (resolves ^15.5.19)
│   ├── react-dom@19.2.7 / react@19.2.7 (deduped)
└── react@19.2.7 · react-dom@19.2.7   (^19.0.0 → already on 19.2 line)
```

## Gate-run log

| # | Commit-ish | tsc | vitest | lint | build | Notes |
|---|---|---|---|---|---|---|
| — | — | — | — | — | — | pending first post-bump run |

## Advisory summary (pre-bump, base 4874a98)

`npm audit --json` metadata: **info 0 · low 0 · moderate 0 · high 3 · critical 0**

All three share one root cause — vulnerable transitive pins inside Next's own
spec; every one reports `fixAvailable: { name: "next", version: "16.3.2",
isSemVerMajor: true }`, i.e. unreachable while staying on 15.x:

1. **postcss** ≤8.5.22 (transitive of next) — HIGH — 4 GHSA advisories:
   GHSA-qx2v-qp2m-jg93 (XSS via unescaped `</style>` in stringify output),
   GHSA-6g55-p6wh-862q (arbitrary file read via attacker-controlled
   sourceMappingURL), GHSA-fxqj-rqcc-2cmp (incomplete fix of the former),
   GHSA-r28c-9q8g-f849 (path traversal in source-map auto-loading).
2. **sharp** <0.35.0 (transitive of next) — HIGH — GHSA-f88m-g3jw-g9cj,
   inherited libvips vulnerabilities CVE-2026-33327 / -33328 / -35590 / -35591.
3. **next** 9.3.4-canary.0 – 16.3.0-preview.10 (direct) — HIGH — flagged *via*
   its postcss + sharp subtree.
